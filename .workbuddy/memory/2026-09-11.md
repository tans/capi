# 2026-09-11

## 新建项目：Capi（参考 runapi.ai 复刻）

**目标**：以 runapi.ai 为参考做一个 AI 模型聚合 API 平台「Capi」。范围＝核心站点，界面与文档尽量复刻；
不做 playground 的真实生成交互；技术栈＝Node 生态 + shadcn/ui。

**技术选型**
- Next.js 16.3.4（App Router / Turbopack）+ React 19 + TypeScript + Tailwind CSS v4。
- shadcn/ui：CLI `init` 被沙箱 SIGKILL，改为**手动配置**（`components.json` + `lib/utils.ts` + 手写
  `components/ui/*`），依赖走 radix 分包。结论：shadcn 组件本身就是复制进项目的源码，手动装配等价。
- 文档渲染：`react-markdown` + `remark-gfm` + `rehype-slug`，配自定义组件映射
  （`pre` → CodeBlock、`blockquote` → 浅蓝 Callout、表格 → Table）。
- 代码高亮：`highlight.js/lib/core` 按语言注册（避免全量包），token 配色对齐参考站的深色编辑器风格。

**架构要点**
- 路由分组：`(marketing)`（带 Header/Footer）、`(auth)`（登录注册）、`(app)`（dashboard）、`docs`（独立三栏壳）。
- 内容层：`lib/models-data.ts`（48 个模型族 / 约 130 个模型 ID）、`lib/api-spec.ts`（41 个端点，结构化数据驱动
  渲染，避免手写数百篇 MDX）、`content/docs/**/*.md`（25 篇，guides 6 + resources 19）+ 自写 frontmatter 解析。
- Mock API：`/api/v1`（chat completions 含 SSE 流、images、kling task、tasks/[id]、models、me/balance），
  内存态任务生命周期 pending→processing→completed，Bearer 鉴权失败返回 401。

**踩坑**
- Editor 对同一文件的连续 Edit 偶发不落盘，改完必须 `grep`/`Read` 复核再 build。
- Next 16 的 `next.config.ts` 改动 `devIndicators: false` 可去掉左下角 dev 徽标，截图时更干净。
- react-markdown 的 `code` 组件只在 fenced block 内出现 `language-*` 类；块级处理要挂在 `pre` 上。

**验证**：`eslint` 0 error；`next build` 通过（50 个模型页 + 41 个 API 页 + 25 个文档页 SSG，营销页静态）。

## 全站 i18n 改造（由另一个并发会话推进）

15:49 起有**另一个会话/进程在同一仓库并发写入**，把全部路由移入 `app/[locale]/`，新增
`middleware.ts`（cookie `CAPI_LOCALE` + Accept-Language 协商）、`lib/i18n/{config,index,server,dictionaries}`、
`app/[locale]/layout.tsx`，并逐个把页面接到词典（顺序：shell 头尾 → docs → 后台壳层 → 前台 `home/*`）。

**踩坑（重要）**：两个会话同时改同一仓库时，整文件回写会让对方改动无声消失 —— 我写入的 12 处后台
词典键在 16:00:21 / 16:00:27 被完全覆盖。**动共享文件前必须先探测**：
`touch /tmp/ref && sleep 30 && find app components lib -type f -newer /tmp/ref`。

**i18n 既定约定（新代码必须遵循）**
- 服务端：`const locale = await resolveLocale(params)`（`lib/i18n/server.ts`，非法值 404）。
- 文案：`getDictionary(locale)`；模板插值用 `interpolate(tpl, { ... })`。
- 链接：一律 `localeHref(locale, path)`，页面里不再出现裸 `/xxx`。
- 客户端组件：由 page/layout 传入 `locale` 属性（组件内拿不到 params）。
- 语言切换：`components/site-header.tsx` 的 `LanguageSwitcher` + `CAPI_LOCALE` cookie。

**后台待办（用户已确认范围：先双语接入，再做真实功能）**
- 未接入 i18n：`app/[locale]/(app)/dashboard/{page,keys,usage,models,settings}`、
  `components/dashboard/{key-manager,settings-form}`（`(app)/layout.tsx` 与 `dash-nav.tsx` 已完成）。
- 需补的后台词典键：`keys.perMonth`；`usage.byKey/dailySpend/total`、`usage.table.{modality,tokens}`、
  `usage.modalities.embeddings`；`models.footnote` 补 `{count}` 占位符（中文串语序依赖它）；
  `settings.{accountId,plan,planValue,notifyWeekly,notifyWeeklyBody}`。

## i18n 改造收尾完成（16:20–16:40）

上面那些待办全部做完了。最终状态：**中英全站切换可用，61/61 路由 200**。

**这轮补齐的部分**
- 营销页 7 个：`mcp / cli / sdk / skills / pricing / teams / privacy` 从零重写为词典驱动
  （此前被删成空文件，是中断时的半成品状态）。
- 后台 5 页 + 2 个客户端组件：`dashboard/{overview,keys,usage,models,settings}` 与
  `key-manager` / `settings-form` 改为接收 `dict` 属性（客户端组件拿不到 params）。
- 认证 2 页 + `auth-form`：同上，改为 `dict` + `localePrefix` 双属性。
- 文档站 4 路由 + 4 组件：`docs/{page,guides,resources,api}`、`docs-shell`、`docs-sidebar`、
  `docs-toc`、`copy-page`、`doc-page-view` 全部接词典；所有站内链接改带 locale 前缀。

**关键决策（新代码请沿用）**
- **`localeHref` 唯一来源是 `lib/i18n/config.ts`**——它会保留 query 与 hash。我一度在 `lib/site.ts`
  里重复实现了一份（会丢 query），已删除，调用点统一改回 config。
- **不要往客户端组件传函数。** 侧边栏搜索的「无结果」文案最初传成 `(q) => tpl.replace(...)`，
  触发 `Functions cannot be passed directly to Client Components`。改成传含 `{query}` 的模板串，
  由客户端替换。
- **中文字距/大写**：`components/marketing/page-hero.tsx` 的 CTA 按 locale 跳过 `uppercase`。
  中文不需要全大写与字距，新加 CTA 组件时注意同样处理。
- **`components/code-block.tsx` 的「复制代码」标签**用 `useSyncExternalStore` 读 `document.documentElement.lang`
  自动适配，不用给每个调用点传 prop。别改回 `useEffect + setState`（会触发 `react-hooks/set-state-in-effect`）。
- **文档正文保持英文**，界面与导航中文化；中文页在正文上方显示 `docs.contentNotice` 说明
  「正文仅英文」。这是有意的范围控制，不是漏翻。
- **模型/端点数据层不翻译**：provider 名、模型 ID、参数名、技术字段（`task_id` 等）保持英文。
  只有展示用的 tagline 走 `lib/models-i18n.ts`。

**踩坑**
- `next build` 与 `next dev` 共用 `.next`，build 前必须先 `pkill -f "next dev"`，否则端口占用报
  「Another next dev server is already running」。
- 删掉根 `app/layout.tsx` 后，静态路由类型缓存会过期，`tsc` 报一堆幽灵错误——`rm -rf .next` 即可消掉。
- 验证用 `/usr/bin/curl` 绝对路径或直接用 Python `urllib`（裸 `curl` 在循环内会 command not found）。
  重定向链的 curl 显示会误导（`/zh/docs` 显示 308 但 urllib 实测 200 → `/zh/docs/guides`）。

**验证**：`eslint` 0 error 0 warning；`tsc --noEmit` 0 error；`next build` 通过
（`[locale]` × 全站静态预渲染，models 为动态渲染）；urllib 全路由扫描 61/61 → 200。

## 后台三块"真功能"（17:05–17:20）

**Key 管理持久化** — `components/dashboard/key-manager.tsx`
- localStorage 双向同步：mount 时一次性 hydrate、之后每次 `setKeys` 都写盘。
- `hydrated` flag 守门 —— 首屏不清掉已存数据，但首屏渲染仍走 seed（避免 SSR 与
  client hydration 不一致），所以视觉上有一闪切换。
- 新增 `import type { Locale }`、函数签名加 `locale: Locale`、创建 Key 的日期用
  `toLocaleDateString(locale === "zh" ? "zh-CN" : "en-GB", …)`；`keys/page.tsx` 同步
  传 `locale`。
- en/zh 词典的 `keys.footnote` 改写为"演示版把密钥保存在浏览器本地——清空站点数据即可清除；
  线上 Capi 只保存哈希值"。

**用量页：模态过滤 + CSV 导出** — `app/[locale]/(app)/dashboard/usage/page.tsx` 与
新建 `components/dashboard/usage-table.tsx`
- 服务端 page 仍负责图表（BarChart + BreakdownBar），把行数据传给客户端 `UsageTable`。
- 客户端组件支持：模态下拉（en/zh 文案走 `t.dashboard.usage.modalities` + 新增
  `usage.filters.{title, modality, all, noResults}` 与 `usage.export.{csv, copied}`）、
  按 modalityKey 过滤、空结果显示、CSV 导出（`Blob` + `<a download>`，文件名
  `capi-usage-<modality>-<date>.csv`），表脚显示「行数 + 请求合计」+ API URL 提示。
- 行数据增加了 `costAmount: number` 字段，给服务端 `aggregateByModality` 与
  `buildByKey` 用来画 breakdown bars（不是 count）。
- byKey 维持原来的 3 个 keys（`video-pipeline` / `web-prod-images` / `internal-tools`）
  的形象化展示，不破坏视觉一致性。

**设置真持久化** — `components/dashboard/settings-form.tsx` 与 `settings/page.tsx`
- 同一套 localStorage 模式：hydrate on mount（`eslint-disable-next-line
  react-hooks/set-state-in-effect` 抑制 set-state-in-effect 警告，因为这是唯一能跨过
  SSR/CSR 边界的做法），changes 写盘。
- 储存 accountName / accountEmail / 每条 notification / savedAt。`useRef` 取 input 值
  + `key={`prop-${hydrated}-${value}`}` 强制 hydration 后重建（避免 sticky 旧值）。
- 按钮现在"按下 = 真的保存 + 更新 savedAt"，后面接 `Intl.RelativeTimeFormat` 自动显示
  "已保存 · 2 分钟前"，30s 自动刷新。
- `settings/page.tsx` 不再自带标题块，改成只渲染 `<SettingsForm dict locale />`。

**新增 mock 端点** — `app/api/v1/me/usage/route.ts`
- Bearer 鉴权复用 `lib/mock-api.ts` 现有 `isAuthorized` / `unauthorized`。
- 查询参数：`modality` (lowercase / 跳过 "all")、`days` ∈ {7,14,30}，非法值回落到 14。
- 返回 `{ account, period_days, modality, totals:{requests,cost}, rows: [...] }`。
- HTML 页脚行直接暴露 `GET /api/v1/me/usage?modality=<当前>&days=14`，让用户能复用。

**验证**：
- `tsc --noEmit`：项目源码 0 error（含之前 4 处类型错误已修复）。
- `eslint --max-warnings 0`：0 error 0 warning。`react-hooks/set-state-in-effect` 警告
  通过两条局部 `eslint-disable-next-line` 抑制（标准 hydration 模式）。
- `next build`：通过；`/api/v1/me/usage` 登记为 `ƒ Dynamic`，所有后台路由仍保持 SSG。
- `curl http://127.0.0.1:3000/api/v1/me/usage`（Bearer ≥ 5 字符）：无 bearer → 401；
  带 bearer → 7 行 + total；`?modality=video` → 2 行；`?days=7` / `?days=banana` → 7/14。
- `/en/dashboard/usage`、`/zh/dashboard/usage`、`/en/dashboard/keys`、`/zh/dashboard/keys`、
  `/en/dashboard/settings` 全部 200，对应 UI 文案（Filter / Export CSV / 筛选 / 导出 CSV /
  吊销即时生效 / 偏好已保存）HTML 中出现。

**踩坑记录**
- "Editor 同一文件连续 Edit 偶发不落盘" 在 17:00 后半段又中招：第一次给
  `key-manager.tsx` 改函数签名 + locale-aware `toLocaleDateString` 两条 Edit 都被
  吃掉了，第二次 Edit 把同一改动再贴一遍才落地。**任何 Edit 都 `grep`/`Read` 复核
  是底线**；改完看不到效果就该怀疑没落盘。
- 端口 3000 上一直有别人的 `next dev`，新改动会被它自动 pick up；新起 `next start`
  在另一端口可能踩到 `.next/server/middleware-manifest.json` 缺失（之前 `rm -rf
  .next` + `next build` 没写完或被抢）。验证用 3000 即可。
- `isAuthorized` 要求 bearer value 长度 > 4——`Bearer x` 直接 401，写测试 / 手册时
  都得用至少 5 字符。

