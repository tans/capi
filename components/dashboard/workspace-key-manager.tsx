"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { getDictionary } from "@/lib/i18n";
import type { Locale } from "@/lib/i18n/config";

export type KeyGroupOption = { name: string; displayName: string };

/** Scopes the relay enforces for workspace keys. */
const KEY_SCOPES = ["llm.chat", "llm.evaluate", "image.generate", "video.generate", "billing.read"] as const;

export function WorkspaceKeyManager({ workspaceId, groups, canManage, locale }: { workspaceId: number; groups: KeyGroupOption[]; canManage: boolean; locale: Locale }) {
  const d = getDictionary(locale).dashboard.workspace.keys;
  const router = useRouter();
  const [name, setName] = useState(""); const [group, setGroup] = useState(""); const [selected, setSelected] = useState<string[]>(["llm.chat"]); const [budget, setBudget] = useState(""); const [secret, setSecret] = useState(""); const [error, setError] = useState(""); const [copied, setCopied] = useState(false); const [busy, setBusy] = useState(false);
  if (!canManage) return null;
  const copy = async () => { await navigator.clipboard.writeText(secret); setCopied(true); setTimeout(() => setCopied(false), 1800); };
  const closeSecret = () => { setSecret(""); router.refresh(); };
  return <section className="card border border-base-300 bg-base-100 shadow-sm"><div className="card-body gap-5"><div><h2 className="card-title text-lg">{d.createTitle}</h2><p className="text-sm text-base-content/60">{d.secretDescription}</p></div><form className="grid gap-4" onSubmit={async (event) => { event.preventDefault(); setError(""); setBusy(true); try { const response = await fetch(`/api/workspaces/${workspaceId}/keys`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, group, scopes: selected.join(","), budget }) }); const data = await response.json(); if (!response.ok) { setError(data.error || d.createError); return; } setSecret(data.secret); setName(""); setBudget(""); router.refresh(); } catch { setError(d.createError); } finally { setBusy(false); } }}><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><label className="form-control"><span className="label-text mb-1">{d.name}</span><input className="input input-bordered" value={name} onChange={(event) => setName(event.target.value)} placeholder={d.namePlaceholder} required /></label><label className="form-control"><span className="label-text mb-1">{d.group}</span><select className="select select-bordered" value={group} onChange={(event) => setGroup(event.target.value)}><option value="">{d.groupDefault}</option>{groups.map((option) => <option key={option.name} value={option.name}>{option.displayName || option.name}</option>)}</select></label><label className="form-control"><span className="label-text mb-1">{d.budget}</span><input className="input input-bordered" inputMode="decimal" value={budget} onChange={(event) => setBudget(event.target.value)} placeholder={d.noLimit} /></label></div><fieldset><legend className="label-text mb-2">{d.permissions}</legend><div className="grid gap-2 sm:grid-cols-2 lg:grid-cols-4">{KEY_SCOPES.map((scope) => <label className="label cursor-pointer justify-start gap-3 rounded-box border border-base-300 px-3 py-2" key={scope}><input className="checkbox checkbox-sm" type="checkbox" checked={selected.includes(scope)} onChange={(event) => setSelected((current) => event.target.checked ? [...current, scope] : current.filter((item) => item !== scope))} /><span>{d.scopeLabels[scope]}</span></label>)}</div></fieldset><button className="btn w-fit justify-self-end" disabled={busy || !name.trim() || selected.length === 0}>{busy ? <span className="loading loading-spinner loading-sm" /> : d.create}</button></form>{error && <div className="alert alert-error" role="alert">{error}</div>}{secret && <div className="alert alert-success items-start"><div className="min-w-0 flex-1"><p className="font-semibold">{d.secretTitle}</p><code className="mt-2 block break-all rounded bg-base-100/70 p-2 text-xs">{secret}</code><p className="mt-2 text-xs">{d.secretOnce}</p></div><div className="flex shrink-0 gap-2"><button className="btn btn-sm" onClick={copy}>{copied ? d.copied : d.copy}</button><button className="btn btn-sm btn-ghost" onClick={closeSecret}>{d.done}</button></div></div>}</div></section>;
}
