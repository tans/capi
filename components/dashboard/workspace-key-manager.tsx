"use client";import {useState} from "react";export function WorkspaceKeyManager({workspaceId,canManage}:{workspaceId:number;canManage:boolean}){const[n,setN]=useState("");const[s,setS]=useState("models:read");const[k,setK]=useState("");if(!canManage)return null;return <form className="card border border-base-300 bg-base-100 p-4" onSubmit={async e=>{e.preventDefault();const r=await fetch(`/api/workspaces/${workspaceId}/keys`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:n,scopes:s})});const d=await r.json();if(r.ok)setK(d.secret)}}><div className="flex flex-wrap gap-2"><input className="input input-bordered input-sm" value={n} onChange={e=>setN(e.target.value)} placeholder="Key name" required/><input className="input input-bordered input-sm" value={s} onChange={e=>setS(e.target.value)} placeholder="Scopes" required/><button className="btn btn-primary btn-sm">Create</button></div>{k&&<code className="mt-2 break-all">{k}</code>}</form>}
