"use client";
import { useState } from "react";
export function ProjectManager({workspaceId,canManage}:{workspaceId:number;canManage:boolean}){const [name,setName]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");setBusy(true);const r=await fetch(`/api/workspaces/${workspaceId}/projects`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name})});if(!r.ok){setError((await r.json()).error||"Unable to create project");setBusy(false);return}location.reload()}if(!canManage)return null;return <div className="card border border-base-300 bg-base-100"><div className="card-body"><h2 className="card-title text-base">Create project</h2><form onSubmit={submit} className="flex flex-wrap gap-2"><input className="input input-bordered input-sm w-64" value={name} onChange={e=>setName(e.target.value)} placeholder="Project name" required maxLength={120}/><button className="btn btn-primary btn-sm" disabled={busy}>{busy?"Creating…":"Create"}</button></form>{error&&<div className="alert alert-error py-2 text-sm">{error}</div>}</div></div>}
