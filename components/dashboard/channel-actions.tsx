"use client";
import { useState } from "react";
import { getDictionary } from "@/lib/i18n";
import type { Locale } from "@/lib/i18n/config";
export function ChannelActions({workspaceId,channelId,status,name,locale}:{workspaceId:number;channelId:number;status:number;name:string;locale:Locale}){const d=getDictionary(locale).dashboard.components.channelActions;const[editing,setEditing]=useState(false),[value,setValue]=useState(name),[error,setError]=useState("");async function act(method:string,body?:object){setError("");const r=await fetch(`/api/workspaces/${workspaceId}/channels${method==='DELETE'?`?id=${channelId}`:''}`,{method,headers:{"Content-Type":"application/json"},body:body?JSON.stringify({id:channelId,...body}):undefined});if(!r.ok){setError((await r.json().catch(()=>({}))).error||d.updateError);return}location.reload()}if(editing)return <div className="flex flex-wrap items-center gap-1"><input className="input input-bordered input-xs w-40" value={value} onChange={e=>setValue(e.target.value)} aria-label={d.channelName}/><button className="btn btn-xs btn-primary" disabled={!value.trim()} onClick={()=>act("PATCH",{name:value.trim()})}>{d.save}</button><button className="btn btn-xs btn-ghost" onClick={()=>setEditing(false)}>{d.cancel}</button></div>;return <div className="flex flex-wrap items-center gap-1"><button className="btn btn-xs btn-ghost" onClick={()=>setEditing(true)}>{d.edit}</button><button className="btn btn-xs btn-ghost" onClick={()=>act("PATCH",{status:status===1?3:1})}>{status===1?d.disable:d.enable}</button><button className="btn btn-xs btn-ghost text-error" onClick={()=>{if(confirm(d.deleteConfirm))act("DELETE")}}>{d.delete}</button>{error&&<span className="text-xs text-error">{error}</span>}</div>}
