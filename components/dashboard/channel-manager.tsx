"use client";
import { useState } from "react";
import { getDictionary } from "@/lib/i18n";
import type { Locale } from "@/lib/i18n/config";
type Props={workspaceId:number;canManage:boolean;allowPlatformChannels:boolean;locale:Locale};
export function ChannelManager({workspaceId,canManage,allowPlatformChannels,locale}:Props){const d=getDictionary(locale).dashboard.components.channels;const[name,setName]=useState("");const[baseUrl,setBaseUrl]=useState("");const[models,setModels]=useState("");const[keys,setKeys]=useState("");const[enabled,setEnabled]=useState(allowPlatformChannels);const[error,setError]=useState("");const[saving,setSaving]=useState(false);async function updateDefaultChannel(next:boolean){setError("");setEnabled(next);const response=await fetch(`/api/workspaces/${workspaceId}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({allowPlatformChannels:next})});if(!response.ok){setEnabled(!next);setError(d.updateError)}}async function create(event:React.FormEvent){event.preventDefault();setError("");setSaving(true);const response=await fetch(`/api/workspaces/${workspaceId}/channels`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name,baseUrl,models,keys,type:"openai-compatible"})});if(!response.ok){setError((await response.json().catch(()=>({}))).error||d.createError);setSaving(false);return}location.reload()}return <div className="flex flex-col gap-4"><section className="rounded-md border border-border bg-card p-4"><div className="flex items-start justify-between gap-4"><div><h2 className="font-medium">{d.defaultTitle}</h2><p className="mt-1 text-sm text-muted-foreground">{d.defaultDescription}</p></div>{canManage&&<input type="checkbox" className="toggle toggle-sm" checked={enabled} onChange={event=>void updateDefaultChannel(event.target.checked)} aria-label={d.enable}/>}</div><p className="mt-3 text-xs text-muted-foreground">{enabled?d.enabled:d.disabled}</p></section>{canManage&&<form onSubmit={create} className="rounded-md border border-border bg-card p-4"><h2 className="font-medium">{d.addTitle}</h2><p className="mt-1 text-sm text-muted-foreground">{d.addDescription}</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><input className="input input-bordered input-sm" placeholder={d.namePlaceholder} value={name} onChange={event=>setName(event.target.value)} required/><input className="input input-bordered input-sm" type="url" placeholder={d.baseUrlPlaceholder} value={baseUrl} onChange={event=>setBaseUrl(event.target.value)} required/><textarea className="textarea textarea-bordered sm:col-span-2" rows={2} placeholder={d.modelsPlaceholder} value={models} onChange={event=>setModels(event.target.value)} required/><textarea className="textarea textarea-bordered sm:col-span-2" rows={2} placeholder={d.keysPlaceholder} value={keys} onChange={event=>setKeys(event.target.value)} required autoComplete="off"/></div>{error&&<p role="alert" className="mt-3 text-sm text-error">{error}</p>}<button className="btn btn-sm btn-primary mt-4" disabled={saving}>{saving?d.saving:d.add}</button></form>}{!canManage&&error&&<p role="alert" className="text-sm text-error">{error}</p>}</div>}
